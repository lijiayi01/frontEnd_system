#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_terser@4.0.0@terser/bin/uglifyjs" "$@"
  ret=$?
else 
  node  "$basedir/../../../_terser@4.0.0@terser/bin/uglifyjs" "$@"
  ret=$?
fi
exit $ret
